---
meta:
  title: Musical Wisdom
  description: A blog sharing deep insights and critiques on music, past and present.
layout: base
eleventyNavigation:
  key: Home
  order: 1
---

<!-- Hero Section -->
<section class="min-h-[80vh] flex items-center py-8 sm:py-12 md:py-16 mx-4 sm:mx-8 md:mx-12 lg:mx-20">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 items-center">

      <!-- Left Content -->
      <div class="space-y-6 md:space-y-8">
        <div class="space-y-4">
          <div class="flex flex-col sm:flex-row items-start sm:items-center gap-2 sm:gap-3">
            <span class="badge badge-soft badge-primary">Philosophy Meets Sound</span>
            <span class="text-sm text-secondary">Exploring music's deeper meaning</span>
          </div>
          <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
            Where <span class="text-primary">Music</span> Meets <span class="text-secondary">Philosophy</span>
            {# <span class="size-20 ml-3 sm:ml-4 inline-block">
              {% include "socrates-icon.njk" %}
            </span> #}
          </h1>
          <p class="text-base sm:text-lg md:text-xl text-secondary/80 max-w-lg">
            Dive deep into the philosophical dimensions of music. From ancient wisdom to modern insights, explore how sound shapes thought and consciousness.
          </p>
        </div>

        <div class="flex flex-col sm:flex-row gap-3 sm:gap-4">
          <a href="/posts/" class="btn btn-soft btn-primary btn-lg w-full sm:w-auto">
            <span class="icon-[tabler--book] size-5 mr-2"></span>
            Explore Articles
          </a>
          <a href="/about/" class="btn btn-soft btn-secondary btn-lg w-full sm:w-auto">
            <span class="icon-[tabler--info-circle] size-5 mr-2"></span>
            About the Blog
          </a>
        </div>

        <div class="flex flex-wrap items-center gap-4 sm:gap-8 pt-4">
          <div class="flex items-center gap-2">
            <span class="text-2xl sm:text-3xl font-bold text-primary">50+</span>
            <span class="text-secondary">Philosophical Essays</span>
          </div>
          <div class="flex items-center gap-2">
            <span class="text-2xl sm:text-3xl font-bold text-secondary">100+</span>
            <span class="text-primary">Musical Insights</span>
          </div>
          <div class="flex items-center gap-2">
            <span class="text-2xl sm:text-3xl font-bold text-primary">Ancient</span>
            <span class="text-secondary">to Modern</span>
          </div>
        </div>
      </div>

      <!-- Right Grid -->
      <div class="grid grid-cols-2 gap-3 sm:gap-4 md:gap-6">
        <div class="relative aspect-square group">
          <img src="https://picsum.photos/400/400?random=1" alt="Classical Philosophy" class="w-full h-full object-cover rounded-lg shadow-lg transition-transform duration-300 group-hover:scale-105">
          <div class="absolute inset-0 bg-primary/30 rounded-lg"></div>
          <div class="absolute bottom-4 left-4 text-white">
            <span class="text-sm font-medium">Ancient Wisdom</span>
          </div>
        </div>
        <div class="relative aspect-square md:mt-8 lg:mt-12 group">
          <img src="https://picsum.photos/400/400?random=2" alt="Modern Music" class="w-full h-full object-cover rounded-lg shadow-lg transition-transform duration-300 group-hover:scale-105">
          <div class="absolute inset-0 bg-secondary/30 rounded-lg"></div>
          <div class="absolute bottom-4 left-4 text-white">
            <span class="text-sm font-medium">Modern Sound</span>
          </div>
        </div>
        <div class="relative aspect-square md:-mt-8 lg:-mt-12 group">
          <img src="https://picsum.photos/400/400?random=3" alt="Musical Theory" class="w-full h-full object-cover rounded-lg shadow-lg transition-transform duration-300 group-hover:scale-105">
          <div class="absolute inset-0 bg-primary/30 rounded-lg"></div>
          <div class="absolute bottom-4 left-4 text-white">
            <span class="text-sm font-medium">Deep Analysis</span>
          </div>
        </div>
        <div class="relative aspect-square group">
          <img src="https://picsum.photos/400/400?random=4" alt="Consciousness" class="w-full h-full object-cover rounded-lg shadow-lg transition-transform duration-300 group-hover:scale-105">
          <div class="absolute inset-0 bg-secondary/30 rounded-lg"></div>
          <div class="absolute bottom-4 left-4 text-white">
            <span class="text-sm font-medium">Consciousness</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Featured Posts Section -->
<section class="py-12 sm:py-16 md:py-20 bg-base-200">
  <div class="container mx-auto px-4 sm:px-8 md:px-12 lg:px-20">
    <h2 class="text-2xl sm:text-3xl font-bold text-center mb-4 sm:mb-6 md:mb-8 text-primary">Latest Philosophical Explorations</h2>
    <p class="text-center text-base-content/70 mb-8 sm:mb-12 md:mb-16">Discover our latest insights where music meets philosophy. <a href="/posts/" class="link link-primary">View all articles</a> to explore more profound connections.</p>

    <!-- Mobile Carousel (hidden on md and up) -->
    <div class="block md:hidden">
      <div
        data-carousel='{
          "loadingClasses": "opacity-0",
          "isSnap": true,
          "isDraggable": true,
          "slidesQty": 1,
          "dotsItemClasses": "carousel-dot carousel-active:bg-primary w-2 h-2 bg-base-300 rounded-full transition-all duration-300"
        }'
        class="relative w-full"
      >
        <div class="carousel overflow-hidden">
          <div class="carousel-body opacity-0">
            {% for post in collections.posts | reverse %}
              {% if loop.index <= 5 %}
                <div class="carousel-slide{% if loop.first %} active{% endif %}" style="width: 100% !important;">
                  <div class="px-6">
                    <div class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 border-0 rounded-2xl overflow-hidden">
                      <figure class="relative h-56 overflow-hidden">
                        {% set firstImage = post.rawContent | matchImage %}
                        <img 
                        src="{{ firstImage.url if firstImage else 'https://picsum.photos/seed/' + post.data.title | slug + '/800/400' }}" 
                        alt="{{ firstImage.alt if firstImage else post.data.title }}"
                        class="w-full h-full object-cover transition-transform duration-300 hover:scale-105"
/>
                        {% if post.data.tags and post.data.tags.length > 0 %}
                          <div class="absolute top-4 left-4 z-10">
                            <span class="badge badge-primary badge-soft text-xs font-medium px-3 py-1">{{ post.data.tags[0] }}</span>
                          </div>
                        {% endif %}
                        {% if site.env == 'development' %}
                        <!-- Comments and Views Overlay -->
                        <div class="absolute bottom-4 right-4 z-10 flex gap-3">
                          <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                            <span class="icon-[tabler--message-circle] size-4 text-white"></span>
                            <span class="text-white text-sm font-medium">{{ post.data.comments.length if post.data.comments else 0 }}</span>
                          </div>
                          <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                            <span class="icon-[tabler--eye] size-4 text-white"></span>
                            <span class="text-white text-sm font-medium">{{ range(150, 2500) | random }}</span>
                          </div>
                        </div>
                        {% endif %}
                        <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
                      </figure>
                      <div class="card-body p-6 space-y-4">
                        <h2 class="card-title text-xl font-bold text-base-content leading-tight line-clamp-2">
                          <a href="{{ post.url }}" class="link link-hover text-primary hover:text-primary/80 transition-colors">{{ post.data.title }}</a>
                        </h2>

                        <div class="flex flex-wrap items-center gap-4 text-sm text-base-content/60">
                          <div class="flex items-center gap-2">
                            <div class="w-6 h-6 bg-primary/10 rounded-full flex items-center justify-center">
                              <span class="icon-[tabler--user] size-3 text-primary"></span>
                            </div>
                            <span class="font-medium">{{ post.data.author }}</span>
                          </div>
                          <div class="flex items-center gap-2">
                            <div class="w-6 h-6 bg-secondary/10 rounded-full flex items-center justify-center">
                              <span class="icon-[tabler--calendar] size-3 text-secondary"></span>
                            </div>
                            <time datetime="{{ post.data.published_date }}" class="font-medium">
                              {{ post.data.published_date | date("MMM d, yyyy") }}
                            </time>
                          </div>

                        </div>

                        <p class="text-base text-base-content/70 line-clamp-3 leading-relaxed">
                          {{ post.data.meta_description }}
                        </p>

                        <div class="flex gap-3 pt-2">
                          <a href="{{ post.url }}" class="btn btn-primary flex-1 gap-2 font-medium">
                            Read
                            <span class="icon-[tabler--arrow-right] size-4"></span>
                          </a>
                          <a href="{{ post.url }}?listen=true" class="btn btn-outline btn-secondary flex-1 gap-2" title="Listen to this article">
                            <span class="icon-[tabler--headphones] size-4"></span>
                            Listen
                          </a>
                        </div>
                      </div>
                    </div>
                  </div>
                </div>
              {% endif %}
            {% endfor %}
          </div>
        </div>
        
        <!-- Navigation Arrows -->
        <button type="button" class="carousel-prev absolute left-2 top-1/2 -translate-y-1/2 carousel-disabled:opacity-30 w-10 h-10 bg-primary/90 hover:bg-primary backdrop-blur-sm shadow-lg hover:shadow-xl flex items-center justify-center rounded-full transition-all duration-200 z-10">
          <span class="icon-[tabler--chevron-left] size-5 text-primary-content"></span>
          <span class="sr-only">Previous</span>
        </button>
        <button type="button" class="carousel-next absolute right-2 top-1/2 -translate-y-1/2 carousel-disabled:opacity-30 w-10 h-10 bg-primary/90 hover:bg-primary backdrop-blur-sm shadow-lg hover:shadow-xl flex items-center justify-center rounded-full transition-all duration-200 z-10">
          <span class="icon-[tabler--chevron-right] size-5 text-primary-content"></span>
          <span class="sr-only">Next</span>
        </button>

        <!-- Pagination Dots -->
        <div class="carousel-pagination absolute -bottom-8 left-0 right-0 flex justify-center gap-3"></div>
      </div>
      
      <!-- Navigation hint -->
      <div class="flex justify-center mt-12 mb-4">
        <div class="text-sm text-base-content/40 flex items-center gap-2 bg-base-200 px-4 py-2 rounded-full">
          <span class="icon-[tabler--swipe] size-4"></span>
          <span>Swipe to explore more articles</span>
        </div>
      </div>
    </div>

    <!-- CSS Override for Full Width -->
    <style>
      @media (max-width: 768px) {
        .carousel-slide {
          width: 100% !important;
          min-width: 100% !important;
          flex: 0 0 100% !important;
        }
      }
    </style>

    <!-- Desktop Layout (hidden on mobile) -->
    <div class="hidden md:block">
      <!-- Top Row: 2 Horizontal Cards -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 sm:gap-8 mb-6 sm:mb-8">
        {% for post in collections.posts | reverse %}
          {% if loop.index <= 2 %}
            <div class="card card-side bg-base-100 shadow-base-300/20 shadow-sm hover:shadow-base-300/40 transition-shadow duration-200 border border-base-300/20 max-w-none">
              <figure class="relative w-48 sm:w-56 flex-shrink-0">
                {% set firstImage = post.rawContent | matchImage %}
                <img 
                src="{{ firstImage.url if firstImage else 'https://picsum.photos/seed/' + post.data.title | slug + '/400/300' }}" 
                alt="{{ firstImage.alt if firstImage else post.data.title }}"
                class="w-full h-full object-cover"
/>
                {% if post.data.tags and post.data.tags.length > 0 %}
                  <div class="absolute top-4 left-4 z-10">
                    <span class="badge badge-primary text-secondary badge-soft">{{ post.data.tags[0] }}</span>
                  </div>
                {% endif %}
                {% if site.env == 'development' %}
                <!-- Comments and Views Overlay -->
                <div class="absolute bottom-4 left-4 z-10 flex gap-3 h-8">
                  <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                    <span class="icon-[tabler--message-circle] size-4 text-white"></span>
                    <span class="text-white text-sm font-medium">{{ post.data.comments.length if post.data.comments else 0 }}</span>
                  </div>
                  <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                    <span class="icon-[tabler--eye] size-4 text-white"></span>
                    <span class="text-white text-sm font-medium">{{ range(150, 2500) | random }}</span>
                  </div>
                </div>
                {% endif %}
              </figure>
              <div class="card-body flex-1">
                <h2 class="card-title text-lg sm:text-xl text-base-content">
                  <a href="{{ post.url }}" class="link link-hover text-primary hover:text-primary/80">{{ post.data.title }}</a>
                </h2>

                <div class="flex flex-wrap items-center gap-4 text-sm text-base-content/70 mt-2">
                  <div class="flex items-center gap-2">
                    <span class="icon-[tabler--user] size-4 text-primary"></span>
                    <span>{{ post.data.author }}</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <span class="icon-[tabler--calendar] size-4 text-primary"></span>
                    <time datetime="{{ post.data.published_date }}">
                      {{ post.data.published_date | date("MMM d, yyyy") }}
                    </time>
                  </div>
                </div>

                <p class="mt-3 text-secondary/80 line-clamp-3 text-sm sm:text-base">
                  {{ post.data.meta_description }}
                </p>

                <div class="card-actions justify-end mt-4">
                  <a href="{{ post.url }}" class="btn btn-primary btn-sm text-primary btn-soft hover:bg-primary-focus">
                  Read More
                  <span class="icon-[tabler--arrow-right] size-4"></span>
                  </a>
                  <a href="{{ post.url }}?listen=true" class="btn btn-secondary btn-soft btn-sm gap-2" title="Listen to this article">
                    <span class="icon-[tabler--headphones] size-4"></span>
                  Listen
                </a>
                </div>
              </div>
            </div>
          {% endif %}
        {% endfor %}
      </div>

      <!-- Bottom Row: 3 Vertical Cards -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8">
        {% for post in collections.posts | reverse %}
          {% if loop.index > 2 and loop.index <= 5 %}
            <div class="card bg-base-100 shadow-base-300/20 shadow-sm hover:shadow-base-300/40 transition-shadow duration-200 border border-base-300/20 max-w-2xl">
              <figure class="relative h-48 overflow-hidden">
                {% set firstImage = post.rawContent | matchImage %}
                <img 
                src="{{ firstImage.url if firstImage else 'https://picsum.photos/seed/' + post.data.title | slug + '/800/400' }}" 
                alt="{{ firstImage.alt if firstImage else post.data.title }}"
                class="w-full h-full object-cover"
/>
                {% if post.data.tags and post.data.tags.length > 0 %}
                  <div class="absolute top-4 left-4 z-10">
                    <span class="badge badge-primary text-secondary badge-soft">{{ post.data.tags[0] }}</span>
                  </div>
                {% endif %}
                {% if site.env == 'development' %}
                <!-- Comments and Views Overlay -->
                <div class="absolute bottom-4 right-4 z-10 flex gap-3 h-8">
                  <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                    <span class="icon-[tabler--message-circle] size-4 text-white"></span>
                    <span class="text-white text-sm font-medium">{{ post.data.comments.length if post.data.comments else 0 }}</span>
                  </div>
                  <div class="flex items-center gap-1 bg-black/60 backdrop-blur-sm rounded-full px-3 py-1">
                    <span class="icon-[tabler--eye] size-4 text-white"></span>
                    <span class="text-white text-sm font-medium">{{ range(150, 2500) | random }}</span>
                  </div>
                </div>
                {% endif %}
              </figure>
              <div class="card-body">
                <h2 class="card-title text-lg sm:text-xl text-base-content">
                  <a href="{{ post.url }}" class="link link-hover text-primary hover:text-primary/80">{{ post.data.title }}</a>
                </h2>

                <div class="flex flex-wrap items-center gap-4 text-sm text-base-content/70 mt-2">
                  <div class="flex items-center gap-2">
                    <span class="icon-[tabler--user] size-4 text-primary"></span>
                    <span>{{ post.data.author }}</span>
                  </div>
                  <div class="flex items-center gap-2">
                    <span class="icon-[tabler--calendar] size-4 text-primary"></span>
                    <time datetime="{{ post.data.published_date }}">
                      {{ post.data.published_date | date("MMM d, yyyy") }}
                    </time>
                  </div>
                </div>

                <p class="mt-4 text-secondary/80 line-clamp-4">
                  {{ post.data.meta_description }}
                </p>

                <div class="card-actions justify-end mt-4">
                  <a href="{{ post.url }}" class="btn btn-primary btn-sm text-primary btn-soft hover:bg-primary-focus">
                  Read More
                  <span class="icon-[tabler--arrow-right] size-4"></span>
                  </a>
                  <a href="{{ post.url }}?listen=true" class="btn btn-secondary btn-soft btn-sm gap-2" title="Listen to this article">
                    <span class="icon-[tabler--headphones] size-4"></span>
                  Listen
                </a>
                </div>
              </div>
            </div>
          {% endif %}
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<!-- Philosophy & Music Themes Section -->
<section class="py-12 sm:py-16 md:py-20">
  <div class="container mx-auto px-4 sm:px-8 md:px-12 lg:px-20">
    <div class="text-center mb-12">
      <h2 class="text-2xl sm:text-3xl font-bold mb-4 text-primary">Explore Philosophical Themes</h2>
      <p class="text-base-content/70 max-w-2xl mx-auto">Discover how music has shaped philosophical thought throughout history and how philosophy illuminates the essence of musical experience.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
      <!-- Ancient Wisdom -->
      <div class="card bg-base-100 shadow-base-300/20 hover:shadow-base-300/40 transition-shadow duration-200 border border-base-300/20">
        <div class="card-body text-center p-6">
          <div class="w-16 h-16 bg-primary/20 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="icon-[tabler--building] size-8 text-primary"></span>
          </div>
          <h3 class="card-title justify-center text-xl mb-3 text-primary">Ancient Wisdom</h3>
          <p class="text-secondary/80 mb-4">Explore how Pythagoras, Plato, and ancient philosophers understood music as fundamental to cosmic order and human consciousness.</p>
          <a href="/posts/tags/ancient/" class="btn btn-soft btn-primary btn-sm">
            Explore Ancient Texts
            <span class="icon-[tabler--arrow-right] size-4 ml-2"></span>
          </a>
        </div>
      </div>

      <!-- Modern Philosophy -->
      <div class="card bg-base-100 shadow-base-300/20 hover:shadow-base-300/40 transition-shadow duration-200 border border-base-300/20">
        <div class="card-body text-center p-6">
          <div class="w-16 h-16 bg-secondary/20 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="icon-[tabler--brain] size-8 text-secondary"></span>
          </div>
          <h3 class="card-title justify-center text-xl mb-3 text-secondary">Modern Thought</h3>
          <p class="text-secondary/80 mb-4">From Nietzsche's philosophy of music to contemporary theories of consciousness and sound, explore modern perspectives.</p>
          <a href="/posts/tags/modern/" class="btn btn-soft btn-secondary btn-sm">
            Modern Insights
            <span class="icon-[tabler--arrow-right] size-4 ml-2"></span>
          </a>
        </div>
      </div>

      <!-- Consciousness & Sound -->
      <div class="card bg-base-100 shadow-base-300/20 hover:shadow-base-300/40 transition-shadow duration-200 border border-base-300/20">
        <div class="card-body text-center p-6">
          <div class="w-16 h-16 bg-primary/20 rounded-full flex items-center justify-center mx-auto mb-4">
            <span class="icon-[tabler--atom-2] size-8 text-primary"></span>
          </div>
          <h3 class="card-title justify-center text-xl mb-3 text-primary">Consciousness</h3>
          <p class="text-secondary/80 mb-4">Delve into how music affects consciousness, meditation, and the nature of perception through philosophical inquiry.</p>
          <a href="/posts/tags/consciousness/" class="btn btn-soft btn-primary btn-sm">
            Explore Consciousness
            <span class="icon-[tabler--arrow-right] size-4 ml-2"></span>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Newsletter & Community Section -->
<section class="py-16 sm:py-20 md:py-24 bg-gradient-to-br from-primary/10 via-secondary/5 to-primary/5">
  <div class="container mx-auto px-4 sm:px-8 md:px-12 lg:px-20">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Main Content -->
      <div class="space-y-6 md:space-y-8 mb-8 md:mb-12">
        <div class="space-y-4">
          <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-base-content">
            Join the <span class="text-primary">Philosophical</span> 
            <span class="text-secondary">Conversation</span>
          </h2>
          <p class="text-lg sm:text-xl text-secondary/80 max-w-2xl mx-auto">
            Subscribe to receive deep insights where music meets philosophy. Join our community of thinkers exploring the profound connections between sound and wisdom.
          </p>
        </div>
        
        <!-- Benefits Grid -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 my-8 md:my-12">
          <div class="flex flex-col items-center text-center space-y-3">
            <div class="w-12 h-12 bg-primary/20 rounded-full flex items-center justify-center">
              <span class="icon-[tabler--mail] size-6 text-primary"></span>
            </div>
            <h3 class="font-semibold text-base-content">Weekly Insights</h3>
            <p class="text-sm text-secondary/70">Deep philosophical essays delivered weekly</p>
          </div>
          <div class="flex flex-col items-center text-center space-y-3">
            <div class="w-12 h-12 bg-secondary/20 rounded-full flex items-center justify-center">
              <span class="icon-[tabler--users] size-6 text-secondary"></span>
            </div>
            <h3 class="font-semibold text-base-content">Thoughtful Community</h3>
            <p class="text-sm text-secondary/70">Connect with fellow philosophical minds</p>
          </div>
          <div class="flex flex-col items-center text-center space-y-3">
            <div class="w-12 h-12 bg-primary/20 rounded-full flex items-center justify-center">
              <span class="icon-[tabler--book] size-6 text-primary"></span>
            </div>
            <h3 class="font-semibold text-base-content">Exclusive Content</h3>
            <p class="text-sm text-secondary/70">Access to subscriber-only essays</p>
          </div>
        </div>
      </div>

      <!-- Newsletter Signup -->
      <div class="max-w-md mx-auto mb-8">
        <form class="flex flex-col gap-3">
          <input 
            type="email" 
            placeholder="Enter your email address" 
            class="input input-bordered w-full bg-base-100 text-base px-4 py-3 rounded-lg border-2 border-base-300 focus:border-primary focus:outline-none transition-colors" 
            required
          >
          <button 
            type="submit" 
            class="btn btn-primary w-full gap-2 font-medium shadow-lg hover:shadow-xl transition-shadow"
          >
            <span class="icon-[tabler--send] size-5"></span>
            Subscribe
          </button>
        </form>
      </div>

      <!-- Trust Indicators -->
      <div class="flex flex-wrap justify-center items-center gap-6 text-sm text-secondary/60">
        <div class="flex items-center gap-2">
          <span class="icon-[tabler--check] size-4 text-primary"></span>
          <span>No spam, ever</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="icon-[tabler--check] size-4 text-primary"></span>
          <span>Unsubscribe anytime</span>
        </div>
        <div class="flex items-center gap-2">
          <span class="icon-[tabler--check] size-4 text-primary"></span>
          <span>Weekly philosophical insights</span>
        </div>
      </div>
    </div>
  </div>
</section>